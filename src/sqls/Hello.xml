<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="post">

<select id="postinfo" parameterType="MemberDto" resultType="PostDto">
	SELECT FROM POST_SEQ, USER_SEQ, CAMERA_SEQ, POST_CONTENT, POST_WDATE, POST_FILEPATH, POST_READCOUNT, POST_POSITION_NAME
	FROM POST
	WHERE user_seq = #{user_seq} 
</select>

</mapper>